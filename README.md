/* Simple code that lets the user choose wether they want to use addition,subtraction,multiplication, or division.
Once the user has decided, they can enter 2 numbers and the program will give the output.
*/
